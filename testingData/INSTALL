INSTALL

1. Install the Apache server with PHP5
2. Install Boost regex library
3. Set the php.ini
  * max execution time >150
  * max input time >120
  * max memory >500M
  * enable_dl = On
4. copy indri_php.so to /usr/lib/php...
5.setup webui location in apache

RUN

1.Run indrid.sh
2.type address in the url